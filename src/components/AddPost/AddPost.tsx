import React from 'react';

const AddPost = () => {
    return (
            <div>
                <input type="text" />
                <button>Add Post</button>
            </div>
    );
};

export default AddPost;